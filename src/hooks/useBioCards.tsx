import { useLandingPages } from './useLandingPages';

// This is a wrapper around useLandingPages to maintain semantic meaning
// while keeping the underlying data structure and logic the same
export const useBioCards = useLandingPages;
